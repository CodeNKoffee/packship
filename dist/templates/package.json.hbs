{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "main": "{{main}}",
  "module": "{{module}}",
  "type": "{{type}}",
  {{#if scripts}}
  "scripts": {{{JSONstringify scripts}}},
  {{/if}}
  "keywords": {{JSONstringify keywords}},
  "author": "{{author.name}} <{{author.email}}>",
  "license": "{{license}}",
  {{#if peerDependencies}}
  "peerDependencies": {{{JSONstringify peerDependencies}}},
  {{/if}}
  {{#if devDependencies}}
  "devDependencies": {{{JSONstringify devDependencies}}},
  {{/if}}
  {{#if dependencies}}
  "dependencies": {{{JSONstringify dependencies}}},
  {{/if}}
  "files": [
    "dist"
  ],
  "_packshipInitialized": true
}